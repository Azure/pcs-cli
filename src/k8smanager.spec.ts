import { IK8sManager, K8sManager } from './k8smanager';
import { Config } from './config';

// jasmine.DEFAULT_TIMEOUT_INTERVAL = 10000000;
describe('sample test for setting up jest', () => {
    test('Checking k8sManager is initialized',  () => {
        // const config = new Config();
        // config.TLS = {
        //     cert: '1234',
        //     fingerPrint: '12345',
        //     key: '1234',
        // };
        // const kubeConfigPath: string = 'C://users//parvezp//.kube//config';
        // const clusterName: string = 'parvezakstest31-cluster';
        // const k8sManager: IK8sManager = new K8sManager('default', clusterName, kubeConfigPath, config);
        // k8sManager.setupDeployment()
        // .then(done)
        // .catch((error) => {
        //     console.log(error);
        // });
    });
  });